export type HeaderLinks = Array<{
  component?: React.ReactNode;
  isDesktop?: boolean;
  isExternal?: boolean;
  isIconOnly?: boolean;
  name?: string;
  url?: string;
}>;

export interface NavProps { }
