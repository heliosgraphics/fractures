{
	"author": "Chris Puska <theta@hey.com>",
	"private": true,
	"scripts": {
		"docs:build": "yarn workspace fractures-dev build",
		"docs:dev": "yarn workspace fractures-dev dev",
		"docs:export": "yarn workspace fractures-dev build && yarn workspace fractures-dev export -o dist",
		"docs:start": "yarn workspace fractures-dev start",
		"docs:lint": "yarn workspace fractures-dev lint",
		"fr:build": "yarn workspace fractures build",
		"fr:test": "NODE_ENV=test yarn workspace fractures test",
		"test": "yarn run fr:test"
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@types/node": "^18.6.4",
		"eslint": "^8.21.0",
		"prettier": "^2.7.1",
		"typescript": "^4.7.4"
	}
}
